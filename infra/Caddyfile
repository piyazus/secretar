# Локальный reverse proxy (LAN). Публичный доступ идёт через Cloudflare Tunnel
# (cloudflared в docker-compose.yml), TLS терминируется на Cloudflare — ACME не нужен.

{
    auto_https off
}

:80 {
    reverse_proxy frontend:3000
}

http://{$N8N_HOST} {
    reverse_proxy n8n:5678
}
